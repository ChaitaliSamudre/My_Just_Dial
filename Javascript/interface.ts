interface Demo {
    cmpy1: string;
    cmpy2: string;
    getFullname? : Function;

    list: Array<Number>;
}

class Person implements Demo {
    fname = 'false';
    lname;
    list = ['a','b',1, 2];

    constructor ( ) {
        getFullname() : 'assssd';
     }
}